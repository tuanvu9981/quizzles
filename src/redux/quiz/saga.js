import { takeEvery, call, all, put, select } from "redux-saga/effects";

import {
    setCurrentQuizId, setDisableNext,
    setOneTrueOption, setMultipleTrueOptions
} from "./actions"

export default function* rootSaga(){
    yield all([
        
    ])
}